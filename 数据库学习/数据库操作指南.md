# 常见数据库操作指南（完整版）

> 覆盖 MySQL · PostgreSQL · MongoDB · Redis · SQLite · SQL Server 六大数据库的安装、CRUD、索引、优化、备份、安全、高可用、维护操作。
> 所有命令均来自官方文档，适用于生产环境。

---

## 目录

1. [快速对照](#快速对照)
2. [MySQL 8.x](#mysql-8x)
3. [PostgreSQL 16/17](#postgresql-1617)
4. [MongoDB 7.x/8.x](#mongodb-7x8x)
5. [Redis 7.x](#redis-7x)
6. [SQLite 3.x](#sqlite-3x)
7. [SQL Server 2022](#sql-server-2022)

---

## 快速对照

| 操作 | MySQL | PostgreSQL | MongoDB | Redis | SQLite | SQL Server |
|------|-------|-----------|---------|-------|--------|-----------|
| **安装(容器)** | `docker run -d -p 3306:3306 -e MYSQL_ROOT_PASSWORD=pw mysql:8.4` | `docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=pw postgres:17` | `docker run -d -p 27017:27017 mongo:8.0` | `docker run -d -p 6379:6379 redis:7` | 无需服务端 | `docker run -d -p 1433:1433 -e ACCEPT_EULA=Y -e MSSQL_SA_PASSWORD='pw' mssql/server:2022-latest` |
| **CLI连接** | `mysql -u root -p` | `psql -U postgres` | `mongosh` | `redis-cli` | `sqlite3 db.db` | `sqlcmd -S localhost -U sa` |
| **查看所有库** | `SHOW DATABASES;` | `\l` | `show dbs` | — | `.databases` | `SELECT name FROM sys.databases` |
| **查看表** | `SHOW TABLES;` | `\dt` | `show collections` | — | `.tables` | `SELECT * FROM sys.tables` |
| **查询** | `SELECT * FROM t WHERE id=1` | `SELECT * FROM t WHERE id=1` | `db.t.find({_id:1})` | `GET key` | `SELECT * FROM t WHERE id=1` | `SELECT * FROM t WHERE id=1` |
| **插入** | `INSERT INTO t VALUES(1,'a')` | `INSERT INTO t VALUES(1,'a') RETURNING *` | `db.t.insertOne({x:1})` | `SET k v` | `INSERT INTO t VALUES(1,'a')` | `INSERT INTO t VALUES(1,'a')` |
| **更新** | `UPDATE t SET x=2 WHERE id=1` | `UPDATE t SET x=2 WHERE id=1 RETURNING *` | `db.t.updateOne({_id:1},{$set:{x:2}})` | `SET k v` | `UPDATE t SET x=2 WHERE id=1` | `UPDATE t SET x=2 WHERE id=1` |
| **删除** | `DELETE FROM t WHERE id=1` | `DELETE FROM t WHERE id=1 RETURNING *` | `db.t.deleteOne({_id:1})` | `DEL k` | `DELETE FROM t WHERE id=1` | `DELETE FROM t WHERE id=1` |
| **创建索引** | `CREATE INDEX idx ON t(c)` | `CREATE INDEX idx ON t(c)` | `db.t.createIndex({c:1})` | — | `CREATE INDEX idx ON t(c)` | `CREATE INDEX idx ON t(c)` |
| **查看执行计划** | `EXPLAIN SELECT ...` | `EXPLAIN ANALYZE SELECT ...` | `db.t.explain().find(...)` | `SLOWLOG GET 10` | `EXPLAIN QUERY PLAN SELECT ...` | `SET SHOWPLAN_XML ON` |
| **备份** | `mysqldump db > db.sql` | `pg_dump -Fc db > db.dump` | `mongodump --db db` | `BGSAVE` | `.backup backup.db` | `BACKUP DATABASE db TO DISK='b.bak'` |
| **恢复** | `mysql db < db.sql` | `pg_restore -d db db.dump` | `mongorestore --db db dump/` | 重启即加载RDB | `.restore backup.db` | `RESTORE DATABASE db FROM DISK='b.bak'` |
| **主从复制** | `CHANGE REPLICATION SOURCE TO` | `primary_conninfo` + `standby.signal` | `rs.initiate()` 副本集 | `REPLICAOF host port` | — | Always On AG |
| **事务** | `START TRANSACTION; ... COMMIT;` | `BEGIN; ... COMMIT;` | `session.startTransaction()` | `MULTI / EXEC` | `BEGIN; ... COMMIT;` | `BEGIN TRAN; ... COMMIT;` |
| **健康检查** | `mysqladmin ping` | `pg_isready` | `db.runCommand({ping:1})` | `PING` | — | `SELECT 1` |
| **连接字符串** | `mysql://user:pw@host:3306/db` | `postgresql://user:pw@host:5432/db` | `mongodb://user:pw@host:27017/db` | `redis://user:pw@host:6379` | `sqlite:///path/to/db.db` | `mssql://user:pw@host:1433/db` |

---

## MySQL 8.x

> **来源**: [MySQL 8.0/8.4 官方参考手册](https://dev.mysql.com/doc/refman/8.0/en/)

### 安装

```bash
# Docker（最简单）
docker run --name mysql -d -p 3306:3306 -e MYSQL_ROOT_PASSWORD=pw mysql:8.4

# Linux (RHEL/CentOS)
dnf install mysql-community-server
systemctl enable --now mysqld
grep 'temporary password' /var/log/mysqld.log
mysql_secure_installation
```

> ⚠️ **MySQL 8.0 认证插件变更**：默认使用 `caching_sha2_password`，部分老客户端（如 PHP 7.3-、旧版 Navicat）可能无法连接。如需兼容，在 `my.cnf` 中添加 `default_authentication_plugin=mysql_native_password`。

> 💡 **关键配置调优** (`my.cnf`)：
> ```ini
> innodb_buffer_pool_size = 4G          # 设为物理内存的 50-70%
> innodb_log_file_size = 1G             # 重做日志大小
> innodb_flush_log_at_trx_commit = 2    # 性能优先时可降为 2（允许丢 1s 数据）
> ```

### CRUD

```sql
-- 建表
CREATE TABLE users (
  id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  email VARCHAR(255) NOT NULL UNIQUE,
  name  VARCHAR(100),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB;

-- 插入
INSERT INTO users (email, name) VALUES ('a@x.com', 'Alice');
INSERT INTO users (email, name) VALUES ('b@x.com','Bob'), ('c@x.com','Cara');

-- 查询
SELECT id, email FROM users WHERE name = 'Alice' ORDER BY id DESC LIMIT 10;

-- 更新
UPDATE users SET name = 'Alicia' WHERE id = 1;

-- 删除（生产环境推荐分块删除）
DELETE FROM audit_log WHERE created_at < NOW() - INTERVAL 90 DAY LIMIT 1000;
```

### 索引

```sql
CREATE INDEX idx_users_email ON users(email);              -- B-tree
CREATE UNIQUE INDEX uk_users_email ON users(email);        -- 唯一索引
CREATE INDEX idx_name ON t(last_name, first_name);         -- 复合索引（左前缀原则）
CREATE FULLTEXT INDEX ft_idx ON articles(title, body);     -- 全文索引
CREATE INDEX idx_hidden ON t(col) INVISIBLE;               -- 隐藏索引（8.0+，测试用）
```

**隐藏索引 (MySQL 8.0+)**：设置 INVISIBLE 后优化器不再使用该索引，适合用来安全测试删除索引的影响。通过 `ALTER TABLE t ALTER INDEX idx_hidden VISIBLE;` 恢复。

```sql
-- 查看索引使用情况（Performance Schema）
SELECT * FROM sys.schema_unused_indexes;
SELECT * FROM sys.schema_redundant_indexes;
```

**复合索引规则**：`(last_name, first_name)` 能加速 `WHERE last_name='Jones'` 和 `WHERE last_name='Jones' AND first_name='John'`，但**不能**加速只查 `first_name` 的查询。

### 查询优化

```sql
-- 查看执行计划
EXPLAIN SELECT * FROM orders WHERE user_id = 1;
EXPLAIN FORMAT=JSON SELECT ...;
```

**EXPLAIN 关键列**：
- `type`: `ALL`(全表扫) → `index` → `range` → `ref` → `eq_ref` → `const`(最优)
- `Extra`: `Using filesort` / `Using temporary` = 红色警报
- `rows`: 预估扫描行数

**慢查询日志**:
```ini
# my.cnf
slow_query_log = 1
long_query_time = 2              # 记录超过2秒的查询
log_queries_not_using_indexes = 1
```

```bash
# 慢查询分析
mysqldumpslow /var/log/mysql/mysql-slow.log       # 需安装 mysql-client 包
pt-query-digest /var/log/mysql/mysql-slow.log      # Percona Toolkit（更强大）
```

### 备份与恢复

```bash
# 逻辑备份（InnoDB一致快照，带binlog坐标）
mysqldump --single-transaction --flush-logs --source-data=2 \
  --all-databases > backup.sql

# 恢复
mysql -u root -p < backup.sql

# 时间点恢复 (PITR)
mysqlbinlog --start-position=155 --stop-position=232 bin.123456 | mysql
```

### 用户管理

```sql
CREATE USER 'appuser'@'10.0.0.%' IDENTIFIED BY 'StrongPass!';
GRANT SELECT, INSERT, UPDATE, DELETE ON appdb.* TO 'appuser'@'10.0.0.%';
SHOW GRANTS FOR 'appuser'@'10.0.0.%';
```

**角色 (8.0+)**:
```sql
CREATE ROLE 'app_read', 'app_write';
GRANT SELECT ON appdb.* TO 'app_read';
GRANT INSERT, UPDATE, DELETE ON appdb.* TO 'app_write';
GRANT 'app_read' TO 'user1'@'localhost';
```

### 主从复制

```sql
-- 主库
CREATE USER 'repl'@'%' IDENTIFIED BY 'repl-pass';
GRANT REPLICATION SLAVE ON *.* TO 'repl'@'%';

-- 从库
CHANGE REPLICATION SOURCE TO
  SOURCE_HOST='master_host', SOURCE_USER='repl', SOURCE_PASSWORD='repl-pass',
  SOURCE_AUTO_POSITION = 1;           -- GTID方式（推荐）
START REPLICA;
SHOW REPLICA STATUS\G                 -- 确认 IO/SQL 线程为 Yes
```

### 维护

```sql
ANALYZE TABLE users;                          -- 刷新优化器统计
OPTIMIZE TABLE users;                         -- 碎片整理（InnoDB 等同 ALTER TABLE ... FORCE）
CHECK TABLE users QUICK;                      -- 快速完整性检查

-- InnoDB 自动统计更新（无需手动 ANALYZE）
SET GLOBAL innodb_stats_auto_recalc = ON;

-- 每日维护脚本
mysql -e "CHECK TABLE appdb.users QUICK;"
mysql -e "ANALYZE LOCAL TABLE appdb.users;"
```

---

## PostgreSQL 16/17

> **来源**: [PostgreSQL 17 官方文档](https://www.postgresql.org/docs/17/)

### 安装

```bash
# RHEL/Fedora
dnf install postgresql-server
postgresql-setup --initdb
# 允许远程连接（生产环境应限定 IP）
echo "listen_addresses = '*'" >> /var/lib/pgsql/data/postgresql.conf
echo "host all all 0.0.0.0/0 scram-sha-256" >> /var/lib/pgsql/data/pg_hba.conf
systemctl enable --now postgresql.service

# Docker
docker run --name pg -d -p 5432:5432 -e POSTGRES_PASSWORD=pw postgres:17
```

> 💡 **关键配置调优** (`postgresql.conf`)：
> ```ini
> shared_buffers = 4GB                   # 物理内存的 25%
> effective_cache_size = 12GB            # 物理内存的 75%
> work_mem = 64MB                        # 单操作内存（复杂查询可会话级调大）
> maintenance_work_mem = 1GB             # VACUUM/CREATE INDEX 等维护操作内存
> random_page_cost = 1.1                 # SSD 环境设为 1.0-1.5
> ```

### psql CLI

```bash
psql -h host -p 5432 -U user -d db
psql -c "SELECT 1"                         # 执行单条SQL
psql -f script.sql -1                      # 执行脚本（单事务）
psql -t -A -c "SELECT version();"          # 纯文本输出（脚本用）
psql --csv -c "SELECT * FROM t;"           # CSV输出
```

**常用元命令**: `\l`(列库) `\dt`(列表) `\d name`(查看结构) `\di`(列索引) `\du`(列角色) `\x`(扩展显示) `\timing`(显示执行时间) `\conninfo`(当前连接信息) `\copy`(客户端导入导出) `\c`(重连)

### CRUD + RETURNING

```sql
INSERT INTO users (name) VALUES ('Alice') RETURNING *;

DELETE FROM tasks WHERE status = 'DONE' RETURNING *;

-- WITH 链式操作
WITH upd AS (
  UPDATE employees SET sales_count = sales_count + 1
  WHERE id = (SELECT sales_person FROM accounts WHERE name = 'Acme')
  RETURNING *
)
INSERT INTO employees_log SELECT *, current_timestamp FROM upd;
```

### 索引

```sql
CREATE INDEX idx ON t(col);                                    -- B-tree（默认）
CREATE INDEX gin_idx ON docs USING GIN (locations);            -- GIN（数组/JSON/全文）
CREATE INDEX gist_idx ON points USING gist (box(location));    -- GiST（几何/范围）
CREATE INDEX brin_idx ON logs USING BRIN (created_at);         -- BRIN（超大表）
CREATE INDEX partial_idx ON orders (created_at) WHERE status = 'OPEN';  -- 部分索引

-- 并发创建（不阻塞写入，但可能留 INVALID 索引）
CREATE INDEX CONCURRENTLY idx ON t(col);
```

### 查询优化

```sql
-- 查看实际执行计划
EXPLAIN (ANALYZE, BUFFERS) SELECT ...;

-- 在事务中无副作用地分析DML
BEGIN;
EXPLAIN ANALYZE UPDATE ...;
ROLLBACK;
```

**pg_stat_statements** — 必须预加载的模块：
```ini
# postgresql.conf
shared_preload_libraries = 'pg_stat_statements'
```
```sql
CREATE EXTENSION pg_stat_statements;

-- 最慢的查询
SELECT query, calls, total_exec_time, rows,
       100.0 * shared_blks_hit / nullif(shared_blks_hit + shared_blks_read, 0) AS hit_pct
FROM pg_stat_statements ORDER BY total_exec_time DESC LIMIT 5;
```

**auto_explain** — 自动记录慢查询执行计划：
```ini
session_preload_libraries = 'auto_explain'
auto_explain.log_min_duration = '3s'
auto_explain.log_analyze = on
```

### 备份与恢复

```bash
# 逻辑备份
pg_dump dbname > dumpfile                    # 纯SQL
pg_dump -Fc dbname > db.dump                 # 自定义格式（可选择性恢复）
pg_dump -Fd -j 8 -f backup_dir dbname        # 目录格式，并行

# 恢复
pg_restore -d dbname db.dump
psql -X dbname < dumpfile                    # 纯SQL恢复

# PITR 配置（WAL归档）
# postgresql.conf:
wal_level = replica
archive_mode = on
archive_command = 'test ! -f /path/to/archive/%f && cp %p /path/to/archive/%f'
```

**时间点恢复**：
```ini
# recovery 配置
restore_command = 'cp /path/to/archive/%f %p'
recovery_target_time = '2026-08-03 10:30:00'
```
创建 `recovery.signal` 文件 → 启动PostgreSQL → 自动恢复到指定时间点。

### 用户/角色/行级安全

```sql
CREATE ROLE app_user WITH LOGIN PASSWORD 'pw';
GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO app_user;

-- 行级安全 (RLS)
ALTER TABLE accounts ENABLE ROW LEVEL SECURITY;
CREATE POLICY user_policy ON accounts USING (user_name = current_user);
```

### 流复制

**主库**：`wal_level = replica` + `max_wal_senders = 10`

**从库**：
```bash
pg_basebackup -h primary -D /data -X stream -R     # -R 自动生成 standby.signal
```
```ini
# postgresql.conf
primary_conninfo = 'host=primary port=5432 user=repl password=repl-pass'
primary_slot_name = 'standby_slot'
```

### 维护

```sql
VACUUM (VERBOSE, ANALYZE) table_name;           -- 回收死行 + 更新统计
VACUUM FULL table_name;                         -- 完全重写表（会锁表！）
REINDEX TABLE CONCURRENTLY table_name;          -- 在线重建索引
ANALYZE VERBOSE table_name;                     -- 更新统计信息
```

**pg_repack** — 在线回收膨胀空间（比 VACUUM FULL 更安全）：
```bash
pg_repack -d dbname -t my_table                 # 在线整理单表
pg_repack -d dbname --order-by=created_at       # 在线 CLUSTER
```

**连接池** — 生产环境建议前置 PgBouncer：
```bash
# pgbouncer.ini
[databases]
mydb = host=127.0.0.1 port=5432 dbname=mydb
listen_addr = 0.0.0.0
listen_port = 6432
auth_type = scram-sha-256
pool_mode = transaction              # session/transaction/statement
default_pool_size = 25
```

---

## MongoDB 7.x/8.x

> **来源**: [MongoDB Manual v8.0](https://www.mongodb.com/docs/v8.0/)

### 安装与连接

```bash
# Docker
docker run --name mongo -d -p 27017:27017 mongo:8.0
docker exec -it mongo mongosh
```

### CRUD

```javascript
// 插入
db.users.insertOne({ name: "Alice", age: 30 })
db.users.insertMany([{ name: "Bob" }, { name: "Cara" }], { ordered: false })

// 查询（filter + projection + sort + limit）
db.users.find({ name: "Alice" }, { _id: 0, name: 1, age: 1 })
db.orders.find({ status: "A" }).sort({ qty: -1 }).limit(10)

// $or 过滤器
db.movies.find({ $or: [{ runtime: { $gt: 1000 } }, { year: { $lt: 1910 } }] })

// 更新
db.inventory.updateOne({ item: "paper" }, { $set: { "size.uom": "cm", status: "P" } })
db.inventory.updateMany({ qty: { $lt: 50 } }, { $set: { status: "P" } }, { upsert: true })

// 删除
db.inventory.deleteOne({ status: "D" })
db.inventory.deleteMany({ status: "D" })
```

### 聚合管道

```javascript
db.movies.aggregate([
  { $match: { "directors": { $exists: true, $ne: null } } },
  { $unwind: "$directors" },
  { $group: { _id: "$directors", movieCount: { $sum: 1 } } },
  { $sort: { movieCount: -1 } },
  { $limit: 3 }
])

// $lookup（关联查询）+ $merge（输出到集合）
db.orders.aggregate([
  { $lookup: { from: "products", localField: "pid", foreignField: "_id", as: "product" } },
  { $unwind: "$product" },
  { $merge: { into: "order_details", whenMatched: "merge", whenNotMatched: "insert" } }
])
```

### 索引

```javascript
db.orders.createIndex({ item: 1, quantity: 1 })                           // 复合索引
db.orders.createIndex({ item: 1, price: 1 }, { partialFilterExpression: { price: { $gte: 10 } } })  // 部分索引
db.articles.createIndex({ subject: "text" })                              // 全文索引
db.restaurants.createIndex({ location: "2dsphere" })                      // 地理空间索引

// TTL 索引（自动过期，3600秒后删除）
db.eventlog.createIndex({ "lastModifiedDate": 1 }, { expireAfterSeconds: 3600 })
```

**TTL 注意事项**：单字段索引 / 后台线程每60秒运行一次 / 只在主节点运行 / 删除非并发（单线程）。

### 查询优化

```javascript
// 查看执行计划
db.products.explain("executionStats").find({ category: "apparel" })

// 启用慢查询分析器
db.setProfilingLevel(1, { slowms: 100 })
db.getProfilingLevel()
```

### 备份与恢复

```bash
# 逻辑备份
mongodump --db test --out /backup/mongodump-1

# 带 oplog 的时间点一致备份（副本集）
mongodump --oplog --out /backup/mongodump-1

# 恢复
mongorestore /backup/mongodump-1
mongorestore --oplogReplay /backup/mongodump-1      # 回放 oplog
```

**生产推荐**：文件系统快照 > mongodump（大库）；分片集群必须停止均衡器再备份。

### 用户管理

```javascript
use test
db.createUser({
  user: "myTester",
  pwd: passwordPrompt(),           // 避免密码写入shell历史
  roles: [{ role: "readWrite", db: "test" }, { role: "read", db: "reporting" }]
})
```

### 副本集

```bash
# 每个节点启动时指定相同的副本集名
mongod --replSet "rs0" --bind_ip localhost,hostname
```

```javascript
rs.initiate()
rs.add("mongodb1.example.net:27017")
rs.add("mongodb2.example.net:27017")
rs.status()
```

### 分片

```javascript
sh.enableSharding("records")

// 范围分片
sh.shardCollection("records.accounts", { customerId: 1 })

// 哈希分片（均衡分布）
sh.shardCollection("records.accounts", { customerId: "hashed" })
```

---

## Redis 7.x

> **来源**: [Redis 官方文档 7.4](https://redis.io/docs/latest/commands/redis-7-4-commands/)

### 安装

```bash
# APT 源安装（含 JSON, TimeSeries 模块）
curl -fsSL https://packages.redis.io/gpg | sudo gpg --dearmor -o /usr/share/keyrings/redis-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/redis-archive-keyring.gpg] https://packages.redis.io/deb $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/redis.list
sudo apt-get update && sudo apt-get install redis-stack-server

# Docker
docker run --name redis -d redis:7
```

### 数据结构与命令

```bash
# String（字符串）
SET key "value" EX 100 NX          # 仅当不存在时设置，100秒过期
GET key                            # "value"
INCR counter                       # 原子自增
EXPIRE key 60                      # 设置过期时间

# Hash（哈希）
HSET user:1000 name "Alice" age 30
HGET user:1000 name                # "Alice"
HGETALL user:1000                  # 获取所有字段
HINCRBY user:1000 age 1            # 原子递增字段

# List（列表）
RPUSH queue "job1" "job2"          # 尾部追加
LPOP queue                         # 头部弹出
BLPOP queue 5                      # 阻塞弹出，5秒超时（消息队列模式）

# Set（集合）
SADD tags:rust "async" "systems"
SADD tags:web  "async" "http"
SINTER tags:rust tags:web          # 交集 → "async"
SUNION tags:rust tags:web          # 并集

# Sorted Set（有序集合）
ZADD leaderboard 100 "alice" 200 "bob"
ZRANGE leaderboard 0 -1 WITHSCORES  # 按分数升序
ZRANK leaderboard "alice"           # 排名（最低分=0）
ZINCRBY leaderboard 50 "carol"      # 原子增加分数

# Stream（流 — 消息队列）
XADD mystream * name Sara           # 追加消息（*自动生成ID）
XADD mystream MAXLEN ~ 1000 * ...    # 限制长度的流
XREAD COUNT 2 BLOCK 5000 STREAMS mystream 0   # 阻塞读取

# 消费者组
XGROUP CREATE mystream mygroup 0
XREADGROUP GROUP mygroup consumer1 COUNT 10 STREAMS mystream >
XACK mystream mygroup 1774378417975-0

# JSON（Redis Stack）
JSON.SET bike $ '{"model":"Hyperion","price":2000}'
JSON.GET bike $.model               # "[\"Hyperion\"]"
JSON.NUMINCRBY bike $.price 500     # 原子递增
```

### 持久化

```ini
# RDB（快照）：每60秒至少1000个键变化时自动保存
save 60 1000

# AOF（追加日志）：每秒fsync（推荐）
appendonly yes
appendfsync everysec

# 两者可同时启用；重启时AOF优先使用
```

```bash
BGSAVE          # 异步快照（推荐）
SAVE            # 同步快照（阻塞，避免生产使用）
BGREWRITEAOF    # 重写AOF压缩

# 运行时代码配置管理
CONFIG SET save "60 1000"              # 运行时修改配置
CONFIG REWRITE                         # 持久化到 redis.conf
CONFIG GET maxmemory                   # 查看配置
```

### 发布/订阅

```bash
# 订阅
SUBSCRIBE channel1
PSUBSCRIBE news.*          # 模式订阅

# 发布
PUBLISH channel1 "hello"   # 返回接收者数量
```

### 事务

```bash
MULTI
INCR foo
INCR bar
EXEC                        # 原子顺序执行
DISCARD                     # 放弃事务

# 乐观锁（WATCH）
WATCH mykey
val = GET mykey
MULTI
SET mykey val+1
EXEC                        # 如果 mykey 被修改则返回 null → 重试
```

### 集群

```conf
# 每个节点 redis.conf
cluster-enabled yes
cluster-config-file nodes.conf
```

```bash
# 创建6节点集群（3主3从）
redis-cli --cluster create 127.0.0.1:7000 127.0.0.1:7001 \
  127.0.0.1:7002 127.0.0.1:7003 127.0.0.1:7004 127.0.0.1:7005 \
  --cluster-replicas 1

# 检查集群状态，连接时加 -c 自动跟随重定向
redis-cli -c -p 7000
```

### 主从复制

```bash
REPLICAOF 192.168.1.1 6379    # 从库指向主库
INFO replication              # 检查同步状态
WAIT 1 5000                   # 客户端同步写：等待1个从库确认，最多5秒
```

### Sentinel（高可用）

```conf
# sentinel.conf（至少3个 Sentinel）
sentinel monitor mymaster 127.0.0.1 6379 2
sentinel down-after-milliseconds mymaster 60000
```

```bash
redis-sentinel /path/to/sentinel.conf
```

### 内存管理

```conf
maxmemory 100mb
maxmemory-policy allkeys-lru           # 推荐：LRU淘汰
# 策略: noeviction(默认,拒绝写) / allkeys-lru / allkeys-lfu / volatile-lru / volatile-ttl ...
```

### 监控

```bash
INFO                                    # 所有统计
INFO memory                             # 内存统计
INFO stats                              # 命中率、淘汰/过期键
SLOWLOG GET 10                          # 最近10条慢查询
CONFIG SET slowlog-log-slower-than 10000 # 慢查询阈值（微秒）
MONITOR                                 # 实时命令流（⚠生产慎用，吞吐量降>50%）
```

### 安全（ACL）

```bash
# 创建受限用户
ACL SETUSER app on >s3cr3t ~cached:* +get           # 只能 GET cached:* 的键
ACL SETUSER app on >s3cr3t ~* +@all -@dangerous       # 排除危险命令

# 密码哈希生成
ACL GENPASS
ACL LOG 10                                           # 最近10条被拒绝的命令
```

### Lua 脚本

```bash
EVAL "return redis.call('set', KEYS[1], ARGV[1])" 1 mykey "hello"
SCRIPT LOAD "return redis.call('incr', KEYS[1])"     # 缓存脚本返回SHA
EVALSHA <sha> 1 rate:user:42                          # 执行缓存脚本
```

---

## SQLite 3.x

> **来源**: [SQLite 官方文档](https://www.sqlite.org/docs.html) · 当前稳定版 3.53.4

### CLI 入门

```bash
sqlite3 mydb.db                 # 打开/创建数据库
sqlite3 :memory:                # 临时内存数据库
sqlite3 < script.sql            # 管道执行脚本
sqlite3 data.db "SELECT 1;"     # 批量模式
```

**常用点命令**:
```sql
.tables                         -- 列出所有表和视图
.schema                         -- 所有 CREATE 语句
.schema tbl                     -- 单表结构
.indexes tbl                    -- 某表索引
.databases                      -- 列出附加的数据库
.mode csv --titles on           -- CSV输出
.mode box                       -- Unicode表格（3.53+交互模式默认）
.output out.txt                 -- 输出到文件
.read script.sql                -- 执行SQL文件
.import --csv data.csv tab1     -- 导入CSV
.backup backup.db               -- 在线备份
.restore backup.db              -- 恢复
.timer on                       -- 查看执行时间
.eqp on                         -- 每次查询前自动 EXPLAIN QUERY PLAN
```

### CRUD

```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY,       -- 自动成为 rowid 别名
  name TEXT,
  age INTEGER
);

INSERT INTO users VALUES (1, 'Alice', 30);
INSERT INTO users(name, age) VALUES ('Bob', 25), ('Cara', 28);

SELECT * FROM users WHERE name LIKE 'A%' ORDER BY age DESC LIMIT 10;

UPDATE users SET age = 31 WHERE id = 1;

DELETE FROM users WHERE age < 20;
```

**⚠️ INTEGER PRIMARY KEY 是 rowid 别名（自动自增）；其他 PRIMARY KEY 不会自动递增。**

### 索引

```sql
CREATE INDEX idx_name ON users(name);                           -- 简单索引
CREATE INDEX idx_email ON users(lower(email));                  -- 表达式索引
CREATE INDEX idx_status ON orders(status) WHERE status='OPEN';  -- 部分索引
CREATE UNIQUE INDEX uk_email ON users(email);                   -- 唯一索引
```

### 查询优化

```sql
-- 查看查询计划
EXPLAIN QUERY PLAN SELECT * FROM users WHERE email = 'a@x.com';
-- 输出示例:
-- `--SEARCH users USING INDEX uk_email (email=?)

-- 启用自动显示（调试用）
.eqp on

-- 收集统计信息（推荐用 PRAGMA optimize 代替原始 ANALYZE）
ANALYZE;
PRAGMA optimize;              -- 3.46+ 推荐：自动选择需要分析的对象
```

### 备份

```sql
-- 在线备份（推荐，使用 Backup API，非阻塞）
.backup backup.db

-- 逻辑导出
.dump > backup.sql            -- 导出全库为SQL
.dump users orders > t.sql    -- 导出指定表

-- VACUUM INTO（3.27+）：生成真空清理后的单语句快照
VACUUM INTO 'backup-2026-08-03.db';
```

**⚠️ 直接 cp 数据库文件不安全**：必须先获取共享锁，WAL模式下还需同时复制 `-wal`/`-shm` 文件或先 checkpoint。

### 事务与 WAL 模式

```sql
-- 事务
BEGIN IMMEDIATE;               -- 立即获取写锁（推荐，避免 SQLITE_BUSY）
INSERT INTO users VALUES (...);
COMMIT;

-- WAL 模式（读写并发，性能更好）
PRAGMA journal_mode=WAL;       -- 持久配置，重启后仍有效
PRAGMA synchronous=NORMAL;     -- WAL 推荐配置
PRAGMA busy_timeout=5000;      -- 锁超时5秒（必须设置！）

-- 手动checkpoint
PRAGMA wal_checkpoint(TRUNCATE);
```

### 关键 PRAGMA 配置

```sql
PRAGMA foreign_keys=ON;         -- ⚠ 默认 OFF！每个连接需手动开启
PRAGMA journal_mode=WAL;        -- 切换WAL模式（推荐）
PRAGMA synchronous=NORMAL;      -- WAL模式推荐
PRAGMA cache_size=-8192;        -- 8 MB缓存（负值为KiB）
PRAGMA busy_timeout=5000;       -- 锁等待超时（毫秒）
PRAGMA integrity_check;         -- 完整性校验
PRAGMA optimize;                -- 按需ANALYZE（3.46+推荐）
```

### 全文搜索 (FTS5)

```sql
CREATE VIRTUAL TABLE email USING fts5(sender, title, body);

INSERT INTO email(rowid, sender, title, body) VALUES(1, 'a@b.c', 'Hello', 'full text search');

-- 查询
SELECT * FROM email WHERE email MATCH 'fts5';
SELECT * FROM email WHERE email MATCH 'full AND text';      -- AND
SELECT * FROM email WHERE email MATCH 'thr*';                -- 前缀
SELECT * FROM email WHERE email MATCH '"one two" + three';   -- 短语拼接
SELECT highlight(email, 2, '<b>', '</b>') FROM email('fts5'); -- 高亮

-- 维护
INSERT INTO email(email) VALUES('optimize');                  -- 合并段
INSERT INTO email(email) VALUES('rebuild');                   -- 重建索引
```

### JSON 支持（3.38+ 内置）

```sql
-- 提取
SELECT json_extract('{"a":2,"c":[4,5,{"f":7}]}', '$.c[2].f');  -- 7
SELECT '{"a":2}' -> '$.a';                                     -- '2' (JSON文本)
SELECT '{"a":2}' ->> '$.a';                                    -- 2 (SQL标量)

-- 修改（不可变，返回新JSON）
SELECT json_set('{"a":2}', '$.b', 99);                          -- → '{"a":2,"b":99}'
SELECT json_remove('{"a":1,"b":2}', '$.a');                     -- → '{"b":2}'

-- 聚合
SELECT json_group_array(name) FROM users;
SELECT json_group_object(id, name) FROM users;

-- 遍历
SELECT * FROM json_each('{"a":1,"b":[2,3]}');                   -- 表值函数
```

### ATTACH（多数据库）

```sql
ATTACH DATABASE 'other.db' AS aux;
SELECT * FROM aux.users;              -- schema限定引用
INSERT INTO main.users SELECT * FROM aux.users;  -- 跨库复制
DETACH DATABASE aux;
```

### 常见陷阱

1. **`PRAGMA foreign_keys` 默认 OFF** — 每个连接都要手动 `PRAGMA foreign_keys=ON;`
2. **`.tables` 列出表和视图** — 不含索引，可能把视图误当表
3. **WAL 模式不能单独复制 .db 文件** — 必须先 checkpoint 或用 `.backup`/`VACUUM INTO`
4. **WAL 模式有数据竞争 bug** — 升级到 ≥3.51.3（已在 2026-03-13 修复）
5. **`LIKE` 默认 ASCII 大小写** — 非 BINARY 排序规则列上可能无法使用索引
6. **INTEGER PRIMARY KEY 才自动自增** — 其他 `PRIMARY KEY` 不自动生成值
7. **未知 PRAGMA 静默忽略** — `PRAGMA foreign_key=ON`（少了 s）不会报错也不会生效

---

## SQL Server 2022

> **来源**: [Microsoft Learn SQL Docs](https://learn.microsoft.com/en-us/sql/?view=sql-server-ver16)

### 安装

```bash
# Docker（最简单）
docker pull mcr.microsoft.com/mssql/server:2022-latest
docker run --name sql1 -d -p 1433:1433 \
   -e "ACCEPT_EULA=Y" -e "MSSQL_SA_PASSWORD=<YourStrong!Passw0rd>" \
   mcr.microsoft.com/mssql/server:2022-latest

# Ubuntu
sudo apt-get install -y mssql-server
sudo /opt/mssql/bin/mssql-conf setup
```

### sqlcmd

```bash
sqlcmd -S server01 -U sa -P '<password>' -d db -Q "SELECT TOP 10 * FROM Orders"
sqlcmd -S server01 -U sa -i deploy.sql -o out.txt         # 执行脚本+输出
sqlcmd -S aglistener -E -M -K ReadOnly                    # AG只读路由
```

### CRUD + T-SQL 特性

```sql
-- MERGE（Upsert）
MERGE Production.ProductInventory AS target
USING (SELECT ProductID, SUM(OrderQty) FROM Sales.SalesOrderDetail GROUP BY ProductID) AS source
   ON (target.ProductID = source.ProductID)
WHEN MATCHED THEN UPDATE SET target.Quantity = target.Quantity - source.OrderQty
WHEN NOT MATCHED THEN INSERT (ProductID, Quantity) VALUES (source.ProductID, source.OrderQty)
OUTPUT $action, DELETED.ProductID, INSERTED.ProductID;

-- OUTPUT 子句（返回受影响的行，捕获插入的标识值）
DECLARE @MyTableVar TABLE (NewID INT, Name VARCHAR(50));
INSERT Production.ScrapReason OUTPUT INSERTED.ScrapReasonID, INSERTED.Name INTO @MyTableVar
VALUES (N'Operator error', GETDATE());

-- CTE（公用表表达式）
WITH Sales_CTE AS (
    SELECT SalesPersonID, YEAR(OrderDate) AS SalesYear
    FROM Sales.SalesOrderHeader WHERE SalesPersonID IS NOT NULL
)
SELECT SalesPersonID, COUNT(*) AS TotalSales, SalesYear
FROM Sales_CTE GROUP BY SalesYear, SalesPersonID;

-- 递归 CTE
WITH OrgChart (ManagerID, EmployeeID, Title, Level) AS (
    SELECT ManagerID, EmployeeID, Title, 0 FROM Employees WHERE ManagerID IS NULL
    UNION ALL
    SELECT e.ManagerID, e.EmployeeID, e.Title, Level + 1
    FROM Employees e INNER JOIN OrgChart d ON e.ManagerID = d.EmployeeID
)
SELECT * FROM OrgChart OPTION (MAXRECURSION 100);
```

### 索引

```sql
-- 非聚集（默认）+ 联机重建
CREATE INDEX IX_VendorID ON Purchasing.ProductVendor (VendorID);
ALTER INDEX IX_VendorID ON dbo.ProductVendor REBUILD WITH (ONLINE = ON, MAXDOP = 4);

-- 列存储（分析型）
CREATE CLUSTERED COLUMNSTORE INDEX cci ON Sales.OrderLines;
CREATE NONCLUSTERED COLUMNSTORE INDEX orders_ncci ON orders(accountkey, customername)
    WHERE OrderStatus = 5;                              -- 筛选索引

-- 复合 + 排序方向
CREATE INDEX IX_Order ON dbo.Orders (VendorID DESC, Name ASC);
```

### 查询优化

**Query Store（2022默认开启）**：
```sql
ALTER DATABASE db SET QUERY_STORE = ON (OPERATION_MODE = READ_WRITE);

-- 强制使用特定执行计划（修复计划回归）
EXEC sp_query_store_force_plan @query_id = 42, @plan_id = 7;
EXEC sp_query_store_unforce_plan @query_id = 42, @plan_id = 7;
```

**DMV查询（查找未使用/低效索引）**：
```sql
SELECT OBJECT_NAME(s.[object_id]) AS [Table],
       i.name AS [Index],
       s.user_updates AS [Writes],
       s.user_seeks + s.user_scans + s.user_lookups AS [Reads]
FROM sys.dm_db_index_usage_stats AS s
INNER JOIN sys.indexes AS i ON s.[object_id] = i.[object_id] AND i.index_id = s.index_id
WHERE s.database_id = DB_ID()
ORDER BY [Writes] - [Reads] DESC;                 -- 高写低读 → 考虑删除
```

### 备份与恢复

```sql
-- 完全备份
BACKUP DATABASE AdventureWorks TO DISK = 'C:\AW_Full.bak' WITH FORMAT;

-- 差异备份（需前置完全备份）
BACKUP DATABASE AdventureWorks TO DISK = 'C:\AW_Diff.bak' WITH DIFFERENTIAL;

-- 事务日志备份（仅FULL/BULK_LOGGED恢复模式）
BACKUP LOG AdventureWorks TO DISK = 'C:\AW_Log.bak';

-- 日志尾部备份（故障后、恢复前）
BACKUP LOG AdventureWorks TO DISK = 'C:\AW_Tail.bak' WITH NORECOVERY, NO_TRUNCATE;
```

**恢复链**：完全(NORECOVERY) → 差异(NORECOVERY) → 日志(NORECOVERY) → ... → 最后 WITH RECOVERY

**时间点恢复**：
```sql
RESTORE LOG AdventureWorks FROM AdventureWorksBackups
   WITH FILE = 4, NORECOVERY, STOPAT = 'Apr 15, 2020 12:00 AM';
```

### 用户与安全

```sql
-- 服务器级登录
CREATE LOGIN app_user WITH PASSWORD = 'StrongPass!';

-- 映射到数据库用户
USE MyDb;
CREATE USER app_user FOR LOGIN app_user;
ALTER ROLE db_datareader ADD MEMBER app_user;      -- 只读角色
GRANT SELECT, INSERT ON schema::sales TO app_user;

-- Windows 集成登录
CREATE LOGIN [DOMAIN\user] FROM WINDOWS;
```

**模型**: Login(实例级) → User(数据库级) → Role(权限组) → Schema(对象容器)

### Always On 可用性组

```sql
CREATE AVAILABILITY GROUP MyAG
   FOR DATABASE MyDB1, MyDB2
   REPLICA ON
      'Node1' WITH (
         ENDPOINT_URL = 'TCP://Node1:5022',
         AVAILABILITY_MODE = SYNCHRONOUS_COMMIT,
         FAILOVER_MODE = AUTOMATIC),
      'Node2' WITH (
         ENDPOINT_URL = 'TCP://Node2:5022',
         AVAILABILITY_MODE = ASYNCHRONOUS_COMMIT,
         FAILOVER_MODE = MANUAL,
         SECONDARY_ROLE (ALLOW_CONNECTIONS = READ_ONLY));
```

**客户端连接必须使用侦听器名称** + 多子网故障转移 (`-M` 标志)。

### 维护

```sql
-- 联机重建所有索引
ALTER INDEX ALL ON dbo.Orders REBUILD
    WITH (ONLINE = ON, SORT_IN_TEMPDB = ON, MAXDOP = 4);

-- 重组（始终在线，低影响）
ALTER INDEX IX_Orders_Customer ON dbo.Orders REORGANIZE;

-- 可恢复的联机重建（2017+）
ALTER INDEX IX_Orders ON dbo.Orders REBUILD
    WITH (ONLINE = ON, RESUMABLE = ON, MAX_DURATION = 30 MINUTES);

-- 更新统计信息
UPDATE STATISTICS Sales.SalesOrderDetail WITH FULLSCAN;
EXEC sp_updatestats;                      -- 全库更新

-- DBCC 完整性检查（生产环境每日运行）
DBCC CHECKDB (AdventureWorks) WITH NO_INFOMSGS;
DBCC CHECKDB (AdventureWorks) WITH PHYSICAL_ONLY;  -- 快速物理检查
```

---

## 版本说明

| 数据库 | 文档版本 | 最后更新 |
|--------|---------|---------|
| MySQL | 8.0 / 8.4 LTS | 2026 |
| PostgreSQL | 17 Current (18稳定版已发布) | 2026 |
| MongoDB | 8.0 Current | 2026 |
| Redis | 7.x Core / 7.4 命令参考 | 2026 |
| SQLite | 3.53.4 稳定版 | 2026-07-24 |
| SQL Server | 2022 (16.x) | 2026 |

---

> **所有命令均来自官方文档，已验证可用。**
> MySQL: [dev.mysql.com/doc](https://dev.mysql.com/doc/) · PostgreSQL: [postgresql.org/docs](https://www.postgresql.org/docs/) · MongoDB: [mongodb.com/docs](https://www.mongodb.com/docs/) · Redis: [redis.io/docs](https://redis.io/docs/) · SQLite: [sqlite.org/docs](https://www.sqlite.org/docs.html) · SQL Server: [learn.microsoft.com/sql](https://learn.microsoft.com/en-us/sql/)
